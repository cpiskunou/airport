<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="by.piskunou.solvdlaba.persistence.CountryRepository">

    <resultMap id="countryMap" type="Country" autoMapping="false">
        <id property="id" column="country_id"/>
        <result property="name" column="country_name"/>
    </resultMap>

    <resultMap id="countryWithCitiesMap" type="Country" autoMapping="false" extends="countryMap">
        <collection property="cities" ofType="City" javaType="java.util.List" autoMapping="false"
                    resultMap="by.piskunou.solvdlaba.persistence.CityRepository.cityMap"/>
    </resultMap>

    <resultMap id="countryWithAirportsMap" type="Country" autoMapping="false" extends="countryMap">
        <collection property="cities" ofType="City" javaType="java.util.List" autoMapping="false"
                    resultMap="by.piskunou.solvdlaba.persistence.CityRepository.cityWithAirportsMap"/>
    </resultMap>

    <select id="findAll" resultType="Country">
        select id as country_id,
               name as country_name
        from countries
    </select>

    <select id="findById" resultType="Country">
        select id as country_id,
               name as country_name
        from countries
        where id = #{id}
    </select>

    <select id="findByName" parameterType="java.lang.String" resultType="Country">
        select id, name from countries where name = #{name}
    </select>

    <select id="findCountryCities" resultMap="countryWithCitiesMap">
        select countries.id as country_id,
               countries.name as country_name,
               cities.id as city_id,
               cities.name as city_name
        from countries left join cities on countries.id = cities.fk_country_id
        where countries.id = #{id}
    </select>

    <select id="findCountryAirports" resultMap="countryWithAirportsMap">
        select countries.id as country_id,
               countries.name as country_name,
               cities.id as city_id,
               cities.name as city_name,
               airports.id as airport_id,
               airports.name as airport_name
        from countries left join cities on countries.id = cities.fk_country_id
                     left join airports on cities.id = airports.fk_city_id
        where countries.id = #{id}
    </select>

    <insert id="create" useGeneratedKeys="true" keyProperty="id">
        insert into countries(name) values(#{name})
    </insert>

    <update id="updateNameById">
        update countries set name = #{name} where id = #{id}
    </update>

    <delete id="removeById">
        delete from countries where id = #{id}
    </delete>

    <select id="isExistsById" resultType="boolean">
        select exists (select from countries where id = #{id})
    </select>

    <select id="isExistsByName" parameterType="java.lang.String" resultType="boolean">
        select exists (select from countries where name = #{name})
    </select>

</mapper>